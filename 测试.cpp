#include<iostream>
#include<stack>
using namespace std;
int main()
{
    template<class T>;
    T data;
    stack<int> Stack;
    while(cin>>typeid.name(data))
    {
        Stack.push(data);
    }
    while(!Stack.empty())
    {
        cout<<Stack.top()<<" ";
        Stack.pop();
    }
    return 0;
}
